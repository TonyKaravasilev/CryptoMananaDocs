const codeBlocks=document.querySelectorAll(".code-block-header + .highlighter-rouge"),copyCodeButtons=document.querySelectorAll(".code-copy-button");copyCodeButtons.forEach((e,o)=>{const c=codeBlocks[o].innerText;e.addEventListener("click",()=>{window.navigator.clipboard.writeText(c),e.classList.add("copied"),setTimeout(()=>{e.classList.remove("copied")},2e3)})});
